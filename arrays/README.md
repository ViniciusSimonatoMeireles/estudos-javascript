## 📌 Métodos de Array

Neste repositório estão exemplos práticos dos principais métodos de array em JavaScript:

- `map`: transformação de dados
- `filter`: seleção de dados
- `reduce`: agregação de dados
- Combinação de métodos (`filter + reduce`)

Todos os exemplos são executados via Node.js no terminal.
